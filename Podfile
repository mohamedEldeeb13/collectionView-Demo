# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'collectionView-Demo' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for collectionView-Demo

  target 'collectionView-DemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'collectionView-DemoUITests' do
    # Pods for testing
  end
pod "Gemini"
end
